{
  "buildCommand": "prisma generate --schema=./prisma/schema.prisma && prisma db execute --file=./prisma/repair-production.sql && next build",
  "installCommand": "npm install",
  "framework": "nextjs"
}
