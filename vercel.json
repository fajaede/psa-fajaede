{
  "buildCommand": "npx prisma generate --schema=./prisma/schema.prisma && npx prisma db push --skip-generate --schema=./prisma/schema.prisma && next build",
  "installCommand": "npm install",
  "framework": "nextjs",
  "env": {
    "DATABASE_URL": "@database_url"
  }
}
