{
  "buildCommand": "prisma db push --skip-generate && prisma generate && next build",
  "installCommand": "npm install",
  "framework": "nextjs",
  "env": {
    "DATABASE_URL": "@psa_database_url"
  }
}
